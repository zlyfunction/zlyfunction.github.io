[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "seamless-existence"
version = "0.1.0"
description = "Realizability of holonomy signatures for seamless parametrization, via k-differentials"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
